# 仅开发：与仓库根目录服务端 HTTP_ADMIN_TOKEN 一致，用于浏览器调用 POST /v1/knowledge-base/replace
# 复制为 apps/web/.env.local 使用；勿将 .env.local 提交到 git
VITE_HTTP_ADMIN_TOKEN=

# 设为 1 时仅渲染设计系统验收屏 DesignSystemSmoke（与移动端对照截图）
# VITE_DS_SMOKE=1
